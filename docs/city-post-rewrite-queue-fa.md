# صف خودکار بازنویسی پست‌های شهری

- زبان خروجی فقط فارسی است؛ ترجمه انجام نمی‌شود.
- سه نمونه قبلی با شناسه‌های `35258`، `35261` و `35264` از صف خارج‌اند.
- حداقل طول خروجی: ۱۰۵۰ کلمه؛ هدف: ۱۲۰۰ تا ۱۵۰۰ کلمه.
- حداقل ۴ و حداکثر ۷ لینک داخلی مرتبط از فهرست استخراج‌شده سایت.
- لینک موجود، تصویر، shortcode، شماره، تلفن و ساختار HTML محافظت می‌شود.
- شباهت مجاز با متن اصلی حداکثر ۶۲٪ است.
- هر ۱۵ دقیقه حداکثر ۵ نوشته پردازش می‌شود و وضعیت در `artifacts/city-rewrite-queue/status.json` ثبت می‌شود.
- خروجی SQL تجمیعی در `artifacts/city-rewrite-queue/rewrite-all-completed.sql` و بازگشت در `rollback-all-completed.sql` قرار می‌گیرد.
- اعمال SQL روی دیتابیس زنده خودکار نیست و باید ابتدا در staging بررسی شود.

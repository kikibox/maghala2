name: Build images-only v2 with dynamic IDs
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ["featured-images/build_images_only_v2.py", ".github/workflows/build-images-only-v2.yml"]
permissions:
  contents: write
concurrency:
  group: navar-images-only-v2
  cancel-in-progress: false
jobs:
  package:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - run: python featured-images/build_images_only_v2.py
      - name: Commit v2 package
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add artifacts/navar-featured-images-only-v2-report.json
          git add -f artifacts/navar-featured-images-only-v2.sql.gz artifacts/navar-featured-images-only-v2-package.zip
          git commit -m "Publish featured-images-only v2 with dynamic IDs"
          git pull --rebase origin main
          git push origin main

name: Generate missing city posts and images
on:
  workflow_dispatch:
    inputs:
      mode:
        description: process, agnes-models-test, initialize, or reset
        required: false
        default: process
  schedule:
    - cron: "17 */4 * * *"
  push:
    branches: [main]
    paths: ["automation/city-content-trigger.txt"]
permissions:
  contents: write
concurrency:
  group: navar-city-content-queue
  cancel-in-progress: true
jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      PYTHONUNBUFFERED: "1"
      AGNES_API_BASE: https://apihub.agnes-ai.com/v1
      AGNES_API_KEY: ${{ secrets.AGNES_API_KEY }}
      AGNES_MODEL: agnes-3.0-flash
      AGNES_IMAGE_MODEL: agnes-image-2.0-flash
      IMAGE_MODEL: agnes-image-2.0-flash
      MIN_WORDS: "1050"
      MIN_INTERNAL_LINKS: "4"
      BATCH_SIZE: "1"
      QUEUE_RUNS: "20"
      POST_DELAY_SECONDS: "10"
      MAX_ATTEMPTS: "2"
      IMAGE_QA_ATTEMPTS: "1"
      IMAGE_QA_MIN_SCORE: "70"
      IMAGE_QA_FAST_MODE: "1"
      IMAGE_QA_REVIEW_KINDS: "1,3"
      CURRENT_IMAGE_POLICY: fast-hard-reject-only-v9
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-python@v5
        with: {python-version: "3.12"}
      - name: Prepare diagnostics
        shell: bash
        run: |
          mkdir -p artifacts/city-content-queue
          : > artifacts/city-content-queue/run.log
          echo "event=${{ github.event_name }} sha=${{ github.sha }} text=$AGNES_MODEL image=$AGNES_IMAGE_MODEL policy=$CURRENT_IMAGE_POLICY runs=$QUEUE_RUNS delay=$POST_DELAY_SECONDS max_attempts=$MAX_ATTEMPTS image_qa_attempts=$IMAGE_QA_ATTEMPTS image_qa_min=$IMAGE_QA_MIN_SCORE fast=$IMAGE_QA_FAST_MODE review_kinds=$IMAGE_QA_REVIEW_KINDS" | tee -a artifacts/city-content-queue/run.log
          test -n "$AGNES_API_KEY" || { echo "ERROR: AGNES_API_KEY missing" | tee -a artifacts/city-content-queue/run.log; exit 1; }
          sed -i "s/ALLOWED_LATIN={'FAQ'}/ALLOWED_LATIN={'FAQ','AFP'}/" automation/reviewed_publish_city_queue.py
          sed -i "s/if x not in ALLOWED_LATIN/if x.upper() not in ALLOWED_LATIN/" automation/reviewed_publish_city_queue.py
          python -m py_compile automation/city_content_queue.py automation/city_content_queue_cloudflare.py automation/reviewed_publish_city_queue.py automation/run_agnes_city_queue.py automation/run_agnes_city_queue_seo.py automation/run_agnes_city_queue_resilient.py automation/run_agnes_city_queue_fast.py automation/run_agnes_city_queue_image_qa.py automation/agnes_json_client.py automation/research_grounding_review.py automation/city_research.py automation/image_prompt_policy.py automation/image_quality_gate.py automation/migrate_legacy_image_artifacts.py automation/import_permalink_fix.py automation/render_city_queue_status.py automation/package_city_content_batches.py automation/faq_policy.py automation/test_agnes_models.py 2>&1 | tee -a artifacts/city-content-queue/run.log
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Install image tooling
        run: python -m pip install --disable-pip-version-check pillow 2>&1 | tee -a artifacts/city-content-queue/run.log
      - name: Run requested mode with live per-post commits
        id: queue
        shell: bash
        run: |
          set +e
          MODE="${{ github.event.inputs.mode || 'process' }}"
          if [ "${{ github.event_name }}" = "push" ]; then MODE="$(sed -n 's/^mode=//p' automation/city-content-trigger.txt | tail -1)"; fi
          echo "mode=$MODE" | tee -a artifacts/city-content-queue/run.log
          if [ "$MODE" = "agnes-models-test" ]; then
            python automation/test_agnes_models.py 2>&1 | tee -a artifacts/city-content-queue/run.log
          elif [ "$MODE" = "reset" ]; then
            python automation/city_content_queue.py --force-init --init-only 2>&1 | tee -a artifacts/city-content-queue/run.log
          elif [ "$MODE" = "initialize" ]; then
            python automation/city_content_queue.py --init-only 2>&1 | tee -a artifacts/city-content-queue/run.log
          else
            python automation/migrate_legacy_image_artifacts.py 2>&1 | tee -a artifacts/city-content-queue/run.log
            python automation/import_permalink_fix.py 2>&1 | tee -a artifacts/city-content-queue/run.log
            python automation/package_city_content_batches.py 2>&1 | tee -a artifacts/city-content-queue/run.log
            python automation/render_city_queue_status.py
            git add -f artifacts/city-content-queue
            if ! git diff --cached --quiet; then
              git commit -m "Reset legacy city images for current policy"
              git pull --rebase origin main
              git push origin main
            fi
            for RUN_INDEX in $(seq 1 "$QUEUE_RUNS"); do
              echo "post_run=$RUN_INDEX/$QUEUE_RUNS" | tee -a artifacts/city-content-queue/run.log
              python automation/run_agnes_city_queue_image_qa.py 2>&1 | tee -a artifacts/city-content-queue/run.log
              CODE=${PIPESTATUS[0]}
              python automation/import_permalink_fix.py 2>&1 | tee -a artifacts/city-content-queue/run.log
              python automation/package_city_content_batches.py 2>&1 | tee -a artifacts/city-content-queue/run.log
              python automation/render_city_queue_status.py
              git add -f artifacts/city-content-queue
              if ! git diff --cached --quiet; then
                git commit -m "Update live Agnes queue status ($RUN_INDEX/$QUEUE_RUNS)"
                git pull --rebase origin main
                git push origin main
              fi
              if [ "$CODE" != "0" ]; then echo "queue_exit_code=$CODE" | tee -a artifacts/city-content-queue/run.log; fi
              if [ "$RUN_INDEX" -lt "$QUEUE_RUNS" ]; then sleep "$POST_DELAY_SECONDS"; fi
            done
          fi
          exit 0
      - name: Commit final diagnostics
        if: always()
        shell: bash
        run: |
          python automation/import_permalink_fix.py 2>/dev/null || true
          python automation/package_city_content_batches.py 2>/dev/null || true
          python automation/render_city_queue_status.py 2>/dev/null || true
          git add -f artifacts/city-content-queue
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Update Agnes reviewed city-content artifacts"
          git pull --rebase origin main
          git push origin main
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: city-content-queue-${{ github.run_number }}
          path: artifacts/city-content-queue/
          retention-days: 14
